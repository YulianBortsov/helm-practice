apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-app-argocdApp.fullname" . }}-test-connection"
  labels:
    {{- include "web-app-argocdApp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-app-argocdApp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
